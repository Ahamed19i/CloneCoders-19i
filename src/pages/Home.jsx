
import React from 'react'
import '../global.css'

const Home = () => {
  return (
    <div className='box-A'>
      <h1>Accueil</h1>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
      <p>
        bash
        Copier le code
        git branch
        Cela listera les branches locales. Tu devrais voir main (ou master) et éventuellement devs si tu l'as déjà créée localement.
      </p>
    </div>
  )
}

export default Home
